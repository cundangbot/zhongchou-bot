# v1.6.4.7 启动时不再刷新频道拼车模板

## 问题原因

v1.6.4.6 在机器人每次启动时都会查询所有 `channel_message_id` 不为空的历史项目，并逐条调用 Telegram 编辑接口刷新频道拼车模板。

这会导致：

- 部署或容器重启后，无业务变化也编辑历史消息；
- 旧消息已经删除时失败；
- 数据库存的是旧媒体详情消息 ID 时失败或误改旧帖；
- 更换过频道、迁移过数据库或机器人缺少编辑权限时失败；
- 管理群收到没有具体原因的“更新频道拼车面板失败”。

## 修改结果

- 删除启动时全量刷新历史频道模板的逻辑；
- 部署、重启不再编辑任何历史频道消息；
- 保留项目真实变化时的单项目更新：人数变化、满员、补票、取消、过期等；
- 模板丢失时继续使用管理员项目详情中的“生成拼车模板”；
- 真实状态更新失败时，管理员告警增加项目、频道、消息 ID 和 Telegram 原始错误原因。

## 数据库

不需要数据库迁移。

## 检查

- Python 全项目编译通过；
- `refresh_public_join_buttons` 已删除；
- 启动函数中没有历史面板刷新调用；
- 项目状态真实变化时的 `update_public_project` 调用仍保留；
- 压缩包完整性检查通过。
