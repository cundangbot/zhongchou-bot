--- app/handlers/start.py	2026-06-11 14:33:39.000000000 +0000
+++ app/handlers/start.py	2026-06-11 14:49:59.911669397 +0000
@@ -2751,14 +2903,6 @@
         support_source_text=call.message.text or call.message.caption or '',
     )
     await state.set_state(AdminContactReply.message)
-    _remember_admin_support_session(
-        call.from_user.id,
-        ticket_id=ticket_id,
-        source_chat_id=call.message.chat.id,
-        source_message_id=call.message.message_id,
-        source_text=call.message.text or call.message.caption or '',
-    )
-
     if private_prompt_ok:
         admin_name = f'@{call.from_user.username}' if call.from_user.username else str(call.from_user.id)
         await call.message.answer(
@@ -2821,6 +2965,7 @@
             if _admin_group_reply_needed(message):
                 await _notify_admin_group(bot, fail_text, reply_markup=contact_admin_keyboard(ticket_id))
             _clear_admin_support_session(actor_id, ticket_id)
+            await _clear_admin_support_session_db(actor_id, ticket_id)
             if state is not None:
                 await state.clear()
             return
@@ -2934,6 +3079,7 @@
     finally:
         if sent_ok:
             _clear_admin_support_session(actor_id, ticket_id)
+            await _clear_admin_support_session_db(actor_id, ticket_id)
         if state is not None:
             await state.clear()
 
