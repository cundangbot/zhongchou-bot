# v1.6.4.11 会员菜单与启动行为检查报告

## 菜单

底部菜单按实际点击顺序排列：

```text
🚗 发起众筹    🔥 热门众筹
💎 会员购买    📋 我的众筹
```

「会员购买」为第三个业务入口。首页说明、ReplyKeyboard、文字处理器、会员页面标题、支付按钮和左侧 `/member` 命令描述均已统一。

## 命令

已删除 `/menu` 和 `/version`：

- 不再注册 Telegram 命令；
- 不再保留消息处理器；
- 不再依赖所谓“强制刷新菜单”；
- 用户发送 `/start` 或正常打开机器人首页时，直接获得当前四按钮菜单。

保留稳定业务命令：`/start`、`/orders`、`/member`。

## 部署和重启

启动代码已永久删除历史发起人预付恢复函数及调用，不再提供可重新开启的环境变量：

- 不扫描历史待预付项目；
- 不自动处理旧项目；
- 不修改任何历史频道拼车模板。

发起人白名单只在“新项目审核通过”的当次业务流程中执行虚拟双车位核验。项目状态从等待预付变为正式拼车时，只更新当前这个新项目的模板，这是实际状态变化，不是启动刷新。

## 检查

- Python 全项目编译；
- 菜单顺序和统一文案；
- `/menu`、`/version` 路由与命令注册清理；
- 启动历史恢复函数、调用和配置清理；
- 发起人双车位白名单审核时自动核验回归；
- 压缩包扁平结构。
