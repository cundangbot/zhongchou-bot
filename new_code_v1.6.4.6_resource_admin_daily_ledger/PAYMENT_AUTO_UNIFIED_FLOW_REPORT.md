# v1.6.4.4 纯自动支付统一流程检查报告

## 1. 最终支付链

```text
购买成功通知
→ 必须命中四种精确车位商品之一
→ 提取 VP
→ 同一 VP 进入串行锁
→ faka 查询一次
→ 核对系统单号、已支付状态、商品类型、金额、支付方式、下单机器人、Telegram 数字用户 ID
→ 保存 verified_payments 已核实付款记录
→ 本地自动绑定或发送动态项目选择页
```

选择项目、重复通知、管理员联系用户等后续动作均复用 `verified_payments`，不会再次查询 faka。

## 2. 四种商品及付款含义

| product_kind | 商品 | 付款含义 | 项目单车位价格 |
|---|---|---|---|
| `seat_30` | 车位支付链接[拼车单车位30元支付链接] | 普通车位或满员后补票 | 30 元 |
| `seat_60` | 车位支付链接[拼车单车位60元支付链接] | 普通车位或满员后补票 | 60 元 |
| `creator_60` | 车位支付链接[发起人双车位60元支付链接] | 发起人双车位 | 30 元 |
| `creator_120` | 车位支付链接[发起人双车位120元支付链接] | 发起人双车位 | 60 元 |

商品内部空格和零宽字符会忽略，但不接受前缀模糊匹配。其他店铺商品即使带 VP，也不会触发查单。

## 3. 三种绑定分支

### 唯一匹配待付票

- 精确校验商品类型、车位金额、车票类型和项目归属。
- 自动入账并发送动态成功卡片。
- 发起人双车位使用专属车主卡片。

### 多张匹配待付票

按钮动态显示：

```text
🚗 P.xxx｜博主｜普通车位
👑 P.xxx｜博主｜发起人双车位
🔓 P.xxx｜博主｜满员后补票
```

用户点击后直接使用已保存的核实付款结果本地绑定。

### 没有待付票

- faka 已先确认 Telegram 数字用户 ID。
- 直接向该用户发送“已锁定付款记录”项目选择页。
- 不再显示多余的“选择要绑定的拼车”中转按钮。
- 候选项目按商品类型筛选。
- 排除已有已支付车票、已有资源权限或已参加的项目。
- 发起人商品只列出本人发起、等待车主预付且单车位价格匹配的项目。

## 4. 动态项目文案

以下页面均从实际订单/项目读取项目编号、博主和描述：

- 普通车位成功页；
- 满员后补票成功页；
- 发起人双车位成功页；
- 多票选择页；
- 无待付票恢复选择页；
- 本地绑定失败恢复页；
- 已付款查询成功页；
- 车票异常状态页；
- 管理员自动验票成功/失败通知。

## 5. 用户与管理员错误边界

用户端统一提示：

```text
⚠️ 付款已经确认，但座位暂时没有完成绑定
━━━━━━━━━━━━━━

项目：P.{动态项目编号}｜{动态博主}
系统正在保护这笔付款，暂时没有重复扣款或丢失。

请点击下方联系小掌柜，我们会根据已经锁定的付款记录继续处理。
```

用户不会看到金额不符、重复 VP、未支付、商品不一致、并发冲突等技术错误。

管理员通知保留真实原因，并带：

```text
💬 查询用户并联系
```

管理员按钮优先使用已保存的用户 ID；仅在查单阶段就失败、尚未保存核实记录时，才回查 faka。

## 6. 私信送达检查

项目选择页发送后：

- 成功：管理员收到“恢复绑定消息已送达用户”；
- 失败：管理员收到“无法私信用户”，包含用户 ID、VP 和联系按钮。

自动上车成功通知发送失败时，订单仍保持已支付，管理员收到主动联系按钮；同一购买通知再次到达时只补发成功卡片，不重复入账。

## 7. 幂等与重启恢复

- 同一 VP 使用进程内串行锁，避免同时查 faka。
- `verified_payments.system_no` 唯一约束防止数据库重复记录。
- 本地分派前把记录原子标记为 `processing`，防止两个任务同时弹选择页。
- `PaymentOrder.faka_system_no` 唯一约束与业务幂等键防止重复入账。
- 调度器恢复 `verified_unbound` 和超时 `processing` 记录。
- 若支付订单已提交但核实记录尚未标记完成，会自动对账为 `bound`。

## 8. 已移除的公开用户流程

- 用户手动提交 VP；
- 用户在空闲聊天直接发送 VP；
- `/pay_订单 VP`；
- “我已支付，去验票”；
- “提交订单号”；
- 用户端金额不符、重复 VP、未支付等旧错误页；
- 旧 `PaymentSubmit` FSM 状态；
- 旧 `pay:auto_pending` / `pay:auto_project` / `pay:auto_recover` 回调；
- 旧自动监听器内重复的候选匹配和后处理代码；
- 无调用的私有辅助函数和重复支付文案函数。

管理员补单、转绑、按 VP 搜索、冷启动验收属于后台应急能力，继续保留。

## 9. 数据库升级

新增迁移：

```text
0005_verified_payments
```

上线前执行：

```bash
alembic upgrade head
```

## 10. 检查结果

已通过：

- Python 全项目编译；
- f-string 兼容检查；
- 四种商品解析测试；
- 非车位商品过滤测试；
- 单次 faka 查单源代码约束检查；
- 选择流程复用已核实付款结果检查；
- 动态项目成功/失败文案检查；
- 旧用户 VP 路由和 FSM 清理检查；
- 顶层重复定义检查；
- 无调用私有辅助函数检查；
- Alembic 单一 head 和迁移链检查。

当前容器未安装项目要求的 aiogram、Telethon、asyncpg，因此未连接真实 Telegram、faka 和 PostgreSQL 做在线端到端支付测试。
